server:
  port: 3000

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/university_app}
    username: ${DB_USERNAME:thaibao}
    password: ${DB_PASSWORD:22042004bao}
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  # profiles:
  #   active: dev

    

